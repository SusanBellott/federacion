import { createVuetify } from "vuetify";
import "vuetify/styles";
import "@mdi/font/css/materialdesignicons.min.css";

// Crea y exporta la instancia directamente
export default createVuetify({
  theme: {
    defaultTheme: "dark",
  },
});
